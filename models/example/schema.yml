
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: test
      description: "A test model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
          - name: user_name
            description: "The username from the current user"
            tests:
                - unique
                - not_null
          - name: user_email
            description: "The email from the current user"
            tests:
                - unique
                - not_null
          - name: user_birth
            description: "The birth from the current user"

